# Dependencies
node_modules/
package-lock.json

# Build and Temp
dist/
bin/
graphify-out/
.gemini/
.DS_Store

# Generic build/ at repo root only — keep electron-app/build/ which holds icon source
/build/

# Build staging — `npm run build` copies ../agent into electron-app/agent/ here, then deletes it
electron-app/agent/

# User Data & Sessions
sessions/
projects/
known-devices.json

# Machine-local / tooling artifacts
.graphify_python
.claude/settings.local.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Tesseract OCR language-model cache (downloaded at runtime)
agent/advanced/narrative/.tesseract-cache/
agent/advanced/narrative/_*.png
